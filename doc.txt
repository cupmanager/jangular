Kanske göra nåt sånt här:
    
    JangularCompiler c = JangularCompiler.builder()
     .withDirectives(dirRepo)
     .withTemplateLoader(loader);
    
    
    CompilerSession s = c.session()
     .withContextClass(AppEvalContext.class)
     .withClassLoader(myOwnClassLoader);
    
    CompiledTemplate t = s.compile("largetest.html", AppScope.class);
    
    
    t.eval(scope, sb, context)
    t.getCompileDuration(TimeUnit.MILLISECONDS)
    t.warnings()
    t.usedDirectives()
    t.generatedClasses()
        